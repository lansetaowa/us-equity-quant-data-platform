name: "dbt_quant"
version: "1.0.0"
config-version: 2

profile: "dbt_quant"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_quant:
    staging:
      +materialized: view
    dws:
      +materialized: table
    ads:
      +materialized: table