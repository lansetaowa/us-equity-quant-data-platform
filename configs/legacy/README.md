# Legacy Configuration

These configuration files belong to the archived Week 2–3 demo pipeline:

```text
universe.yml
pipeline.yml
```

They are retained so the legacy scripts remain understandable and testable.

They are not used by the formal Week 8 bootstrap or the Week 8.5 windowed
daily-update workflow.

Current pipeline configuration lives primarily in:
```
configs/security_master.yml
configs/backfill.yml
configs/price_update.yml
configs/cloud.yml
```